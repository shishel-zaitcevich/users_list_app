# React + RTK Query

### Стек:

- Vite + React + TypeScript
- Redux Toolkit (RTK Query) — работа с API
- React Router v6
- SCSS-модули
- Lazy Loading + Suspense, useMemo (на случай, если список пользователей станет большим)
- ESLint + Prettier

### Установка:

npm install
npm run dev

### Деплой:

https://users-list-app-mauve.vercel.app/
